import React from "react";
import ThreeCanvas from "@/components/others/customizer-do-not-touch/ThreeCanvas"; // Import the ThreeCanvas component

const Customizer = () => {

  return (
    <div>
      < ThreeCanvas />
    </div>
  );
};

export default Customizer;
